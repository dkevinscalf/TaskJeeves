<Window x:Class="TaskJeeves.Loading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskJeeves"
        mc:Ignorable="d"
        Title="Loading" Height="247" Width="300" Icon="Images/TaskJeeves.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="Images/TaskJeeves.png" />
        <TextBlock Grid.Row="1" Name="txtLoadingStatus" Text="Loading.."/>
        <ProgressBar Name="prgLoadingBar" Grid.Row="2" />
    </Grid>
</Window>
